//// Created by alice on 2023/1/30.//#include <printf.h>#include "CPointer.h"#define SIZE 4void sum_rows(int ar[][COLS], int rows) {    int r;    int c;    int tot;    for (r = 0; r < rows; r++) {        tot = 0;        for (c = 0; c < COLS; c++){            tot += ar[r][c];            printf("row %d: sum = %d\n", r, tot);        }    }}void sum_cols(int ar[][COLS], int rows) { //省略形参名    int r;    int c;    int tot;    for(c = 0; c < COLS; c++) {      tot = 0;      for (r = 0; r < rows; r++) {          tot += ar[r][c];          printf("row %d: sum = %d\n", r, tot);      }    }}int point1() {    short dates[SIZE];    short* pti;    short index;    double bills[SIZE];    double * ptf;    pti = dates;    ptf = bills;    printf("%23s %15s\n", "short", "double");    for (index = 0; index < SIZE; index++) {        printf("pointers + %d: short : %10p, double : %10p\n", index, pti+index, ptf+index);    }    return 0;}int point2() {    return 0;}int test_2() {    int junk[ROWS][COLS] = {            {2, 4, 6, 8},            {3, 5, 7, 9},            {12, 10, 8, 6}    };    sum_rows(junk, ROWS);    sum_cols(junk, ROWS);    return 0;}void test1() {   // int* pi = (int*)malloc(sizeof(int));    //int i = 0x16b16b31c;    //int *pi = (int*)0x16b16b31c;//    int num = 10;//    printf("- num - = %p\n ", &num);//    int *pi = &num;//    printf("- numpi - = %d\n ", *pi);//    *pi = 100;    //printf("- pi - = %d\n ", *pi); //取地址 0x16b16b31c}